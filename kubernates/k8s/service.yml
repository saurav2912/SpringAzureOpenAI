apiVersion: v1
kind: Service
metadata:
  name: springazureopenai-service
spec:
  type: LoadBalancer
  selector:
    app: springazureopenai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080